<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Airbnb Home</title>
    <link rel="stylesheet" href="global.css" />
    <link href="output.css" rel="stylesheet" />
  </head>
  <body
    class="bg-gray-100 flex flex-col items-center min-h-screen text-center px-4 relative"
  >
    <!-- Airbnb Logo as Background
    <div
      class="absolute inset-0 bg-cover bg-center opacity-10"
      style="background-image: url('airbnb-logo.png')"
    ></div> -->

    <!-- Header at Top with Buttons -->
    <%- include('partials/nav') %>

    <!-- Listing Section -->

    <!-- <main class="mt-10 w-full max-w-3xl bg-white p-6 rounded-lg shadow-lg">
      <h2 class="text-2xl font-semibold text-gray-700 mb-4">🏠 Available Homes</h2>
      <ul class="space-y-3 text-left">
        <% homeRegistered.forEach(home => { %>
          <li class="p-4 bg-gray-50 border-l-4 border-red-500 rounded-md shadow-md hover:scale-105 hover:shadow-lg transition-all duration-300 ease-in-out">
            <span class="font-semibold text-gray-800"><%= home.houseName %></span>
          </li>
        <% }) %>
      </ul>
    </main> -->

    <main class="w-full max-w-2xl mx-auto mt-10 px-4">
      <ul class="space-y-4">
        <% for(let i = 0; i < homeRegistered.length; i++) { %>
        <li
          class="flex items-center space-x-4 text-lg font-semibold text-gray-800 bg-white p-4 rounded-lg shadow-md border-l-4 border-red-500 transition-transform duration-300 transform origin-left hover:scale-[1.02] hover:shadow-lg"
        >
          <span class="text-red-500 text-xl font-bold">
            &nbsp;<%= i + 1 %>.</span
          >
          <span class="flex-1"><%= homeRegistered[i].houseName %></span>
        </li>
        <% } %>
      </ul>
    </main>
  </body>
</html>
